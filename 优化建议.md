# 干细胞档案管理系统优化建议

> 说明：本文件仅整理可优化点，**不涉及代码修改**。建议优先评估“高优先级”项。

## 结论速览
- **高优先级**：影响性能/正确性的点，优先评估。
- **中优先级**：影响稳定性/长期维护成本，建议逐步改进。

---

## 高优先级（性能/正确性）

### 1) 分页统计查询被浪费（可能统计不准）
**现象**  
`executeQuery` 对 `SELECT` 只返回 `result.recordset`，但部分路由使用“多条 SELECT 合并一个 query”的写法，导致后续统计结果丢失。  

**影响**  
分页总数可能不准确；数据库多执行了一条查询但结果没被使用。  

**涉及文件**  
- `backend/config/database.js`
- `backend/src/routes/reports.js`

**建议方向**  
拆分为两次查询，或让 `executeQuery` 在多语句时返回 `recordsets`。

---

### 2) 报告列表接口存在 N+1 查询（性能）
**现象**  
体检报告搜索先查 ID 列表，然后对每个 ID 再单独查询日期与科室数。  

**影响**  
数据量增大时接口显著变慢。  

**涉及文件**  
- `backend/src/routes/reports.js`（`/reports/exams`）

**建议方向**  
SQL 一次性聚合，或引入批量查询/缓存。

---

### 3) 对比报告生成串行读取（性能）
**现象**  
对比报告对每个体检 ID 逐个查询 HealthAssessments + LaboratoryData。  

**影响**  
体检记录多时生成明显变慢。  

**涉及文件**  
- `backend/src/routes/reports.js`（`/comparison/generate`）

**建议方向**  
改为批量查询（`IN (...)`）或并发+限流。

---

### 4) 客户删除逻辑重复且缺少事务（正确性/性能）
**现象**  
代码手动删除十余张表数据，同时数据库中部分外键已有 `ON DELETE CASCADE`。  

**影响**  
重复删除 + 多次 DB 往返；失败时难以保证一致性。  

**涉及文件**  
- `backend/src/models/Customer.js`
- `database/03-indexes-and-constraints.sql`

**建议方向**  
依赖外键级联，或用存储过程 + 事务统一删除。

---

## 中优先级（稳定性/维护成本）

### 5) 动态限流器每次请求创建
**现象**  
`createDynamicLimiter()` 在每个请求里 new limiter；`createUserRateLimiter` 的 Map 无清理逻辑。  

**影响**  
性能浪费；长时间运行可能增长内存。  

**涉及文件**  
- `backend/src/middleware/rateLimiter.js`

**建议方向**  
按角色预建 limiter 或使用 Redis store；实现有效清理。

---

### 6) 生产日志过多
**现象**  
`Customer.findAll()` 和部分路由含大量 `console.log`。  

**影响**  
生产 IO 压力大；日志噪音多。  

**涉及文件**  
- `backend/src/models/Customer.js`
- `backend/server.js`

**建议方向**  
统一 logger，并用环境变量控制日志等级。

---

### 7) AI 请求/响应完整落库（成本/安全）
**现象**  
将完整 `apiRequest` / `apiResponse` 存入数据库。  

**影响**  
数据体积大、成本高，且存在敏感信息风险。  

**涉及文件**  
- `backend/src/services/deepseekService.js`

**建议方向**  
只保存必要字段；敏感/大字段脱敏或单独存储。

---

### 8) 体检ID字段命名不一致
**现象**  
数据库新增 `ExamId` 字段，代码中大量使用 `MedicalExamID`。  

**影响**  
易出现查询不走索引或潜在错误。  

**涉及文件**  
- `database/06-laboratory-data-table.sql`
- `backend/src/routes/reports.js`

**建议方向**  
统一字段命名并建立对应索引。

---

## 前端优化建议

### 9) API 缓存参数未真正参与请求
**现象**  
缓存 key 使用 `params`，但 `fetch` URL 未拼接参数。  

**影响**  
缓存命中可能不正确；请求与缓存不一致。  

**涉及文件**  
- `frontend/js/api-cache.js`

**建议方向**  
对 GET 请求统一拼接 query 参数，维护命中率统计。

---

### 10) 缓存无容量上限
**现象**  
缓存只有 TTL，没有条目上限。  

**影响**  
页面长时间运行可能堆积。  

**涉及文件**  
- `frontend/js/api-cache.js`

**建议方向**  
加入简单 LRU 或最大条目限制。

---

### 11) 资源预加载可能重复
**现象**  
运行时注入 `preload/prefetch`，若 HTML 已配置，可能重复。  

**影响**  
浪费带宽、抢占关键资源。  

**涉及文件**  
- `frontend/js/performance.js`

**建议方向**  
预加载在 HTML 统一控制或延迟到空闲时执行。

---

### 12) API 调用封装重复
**现象**  
`ApiService` 与 `fetchWithAuth` 并存，处理逻辑重复。  

**影响**  
维护成本高，容易出现行为不一致。  

**涉及文件**  
- `frontend/js/api.js`

**建议方向**  
统一请求入口、错误处理与超时逻辑。

---

## 可选：建议评估顺序（影响/成本）
1. 分页统计查询逻辑  
2. 报告列表 N+1 查询  
3. 对比报告串行查询  
4. 客户删除逻辑统一/事务化  
5. 限流器与日志  
6. 前端缓存与预加载  

---

如需我输出“按改动成本排序”的方案或出一版更具体的落地方案，请告诉我。
